---
interface Props {
	date: string;
}

const { date } = Astro.props;
import { format} from 'date-fns';
---

<time class="text-muted-foreground font-mono text-sm md:text-base">
	{
		format(date, 'yyyy/MM/dd')
	}
</time>
