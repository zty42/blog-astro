---
interface Props {
	date: Date;
}

const { date } = Astro.props;
import { format } from 'date-fns';
---

<time class="text-gray-500 font-mono" datetime={date.toISOString()}>
	{
		format(date, 'yyyy/MM/dd')
	}
</time>
