---
interface Props {
	date: Date;
}

const { date } = Astro.props;
import { format } from 'date-fns';
---

<time class="text-muted-foreground font-mono text-sm tracking-tight">
	{
		format(date, 'yyyy-MM-dd')
	}
</time>
